"""Tuples are data structure that can never be changed
Maintains insertion order"""

vowels = ('a,', 'e', 'i', 'o', 'u')
vowels[3] = 'f'
print(vowels)

"""Point to note
in order for a tuple to be a tuple, every tuple needs to include 
at least one comma between the parentheses,
 even when the tuple contains a single object."""


